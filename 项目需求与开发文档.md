# 电脑使用情况监控工具 —— 需求与开发文档

- 文档版本：v2.0（与实现对齐修订）
- 编写日期：2026-08-08（v1.0） · 修订日期：2026-09-20（v2.0）
- 项目性质：独立 Windows 系统工具（不依赖 Codex / 不依赖任何 AI 客户端）
- 当前实现版本：v2.9.12（仓库 <https://github.com/Niangaol/VibeTrace>）
- 数据根目录：由 `config.json` 的 `data_root` 决定（空 = 程序所在目录；不再硬编码盘符）

> v2.0 修订说明：保留 v1.0 全部需求原文作为意图基准，逐节标注**实现状态**；
> 「待确认问题清单」修订为已解答状态；模块划分、交付物、验收标准按现状更新。

---

## 1. 项目概述

开发一个**独立的 Windows 后台监控工具**，常驻运行、性能占用极低，自动记录用户每天在电脑上：
- 使用了哪些软件、分别使用了多久（基础监控）；
- 在微信、QQ、钉钉、浏览器中的具体活动（社交软件深度监控）；
- 在 vibe coding（AI 编程）工具中的使用情况（opencode、pi agent、ChatGPT 桌面版等）。

所有数据**只保存在本机**，不上传任何云端，不做截屏、不读聊天内容。

**实现状态：✅ 已交付（v1.0.0 起，持续演进至 v2.9.12）。**

---

## 2. 目标用户与使用场景

1. 用户想复盘"我今天用了什么软件，各用了多久"；
2. 用户想知道"我今天在微信里跟谁聊了多久、浏览器里看了多久视频 / 写了多久代码 / 学了多久习"；
3. 用户使用 opencode、pi agent、ChatGPT、Cursor 等 AI 编程工具，想统计"今天 AI 编程用了多久"；
4. 用户希望每天自动生成一份当天的使用日志/报告，放在当天的日期文件夹里，可长期查看。

**实现状态：✅ 全部覆盖**，并扩展了周报/月报、本地网页仪表盘（十三视图）、AI 会话深度统计、成本与成长分析等（见第 6 节）。

---

## 3. 术语定义

| 术语 | 含义 |
|---|---|
| 会话（Session） | 用户持续停留在同一个"应用 + 窗口上下文"的时间段 |
| 前台窗口 | Windows 当前获得焦点的窗口（`GetForegroundWindow`） |
| 空闲（Idle） | 键盘/鼠标超过阈值（默认 3 分钟）无输入的状态 |
| 类别（Category） | 对软件/网页活动的分类：AI编程、社交聊天、浏览器、开发工具等 |
| 联系人（Contact） | 从微信/QQ/钉钉窗口标题解析出的聊天对象 |
| vibe coding | 使用 AI 编程工具（opencode、pi agent、ChatGPT、Cursor 等）进行开发 |

---

## 4. 运行环境

- Windows 10 / Windows 11（64 位，x64）；
- 单用户登录会话（不需要跨会话）；
- 运行时：Python 3.10+（源码方式）或 PyInstaller 单文件 exe（交付方式，约 12MB）；
- 默认**无需管理员权限**（管理员模式为可选增强）；
- 无网络依赖（离线可用；可选 AI 洞察需用户显式配置端点）。

**实现状态：✅ 一致。**

---

## 5. 总体架构

\`\`\`mermaid
flowchart LR
    A[守护进程<br/>每5秒轮询前台窗口] --> B[Win32 采集<br/>GetForegroundWindow / GetWindowText / GetLastInputInfo]
    B --> C[会话状态机<br/>变化才写一条]
    C --> D[每日数据目录<br/>YYYY-MM-DD/]
    E[软件清单扫描<br/>注册表/开始菜单/进程] --> F[软件清单 + 自动分类]
    F --> D
    D --> G[日报/周报/月报 report.md / report.csv]
    G --> H[托盘图标：今日概览/打开仪表盘/暂停/退出]
    G --> I[CLI 查询 report.py]
    G --> J[本地网页仪表盘 127.0.0.1]
    K[任务计划程序<br/>登录自启] --> A
\`\`\`

### 5.1 模块划分（当前实现）

| 模块 | 职责 |
|---|---|
| `monitor.py` | 守护进程：轮询前台窗口、会话状态机、写 `usage.jsonl`、跨天聚合、托盘调度、`--admin` |
| `win32core.py` | Win32 API（ctypes）：前台窗口 / 进程树 / 空闲 / UWP / 管理员检测 |
| `classifier.py` | 分类引擎：类别 / 联系人 / AI 工具 / 终端工具 / 黑名单 / 配置加载 |
| `inventory.py` | 软件清单扫描（注册表 / 开始菜单 / 运行进程）+ 自动分类 |
| `browser_history.py` | 浏览器 URL 级历史解析（Chromium 系 + Firefox，免拷贝直读源库） |
| `ai_sessions.py` | AI 会话深度统计（轮次 / Token / 成本 / 质量评分） |
| `report.py` | 日报/周报/月报聚合、重分类、verify/repair（含 SQLite 快速路径） |
| `dashboard.py` / `dashboard_util.py` | 本地网页仪表盘（仅 127.0.0.1）+ 全部 `/api/*` 路由 / 其纯函数外置 |
| `insights.py` | 智能洞察：离线规则引擎 + 个性化基线 + 可选 AI 洞察 |
| `derived.py` | 派生指标共用取数框架（`day_bundle` / `series`，v2.9.12）：13 处逐日循环的单一实现 |
| `metrics_util.py` | 统计辅助单一来源（熵 / HHI / 切换计数 / 维度合并 / 格式化） |
| `tool_registry.py` | 工具注册表（唯一事实源：默认目录 / 解析器 / 缓存豁免 / Web 域名） |
| `git_insights.py` | Git 代码变更分析（区间批量 `range_batch`） |
| `growth.py` / `query.py` / `tool_compare.py` / `advice.py` | 成长曲线 / 受限模板查询 / 多工具对比 / 概览建议栏位 |
| `adoption.py` | Git 侧采纳率代理指标（只读，免责+折叠展示） |
| `budget.py` / `alerts.py` / `goals.py` / `learn.py` / `timeline.py` | 预算告警 / 告警调度 / 每日目标 / 在线基线 / 时间轴回放 |
| `sqlite_store.py` | 可选 SQLite 后端 `usage.db`（JSONL 仍为原始事实源）+ 一致性校验 |
| `updater.py` | 新版本检测、应用内更新、下载地址白名单 |
| `tray.py` | 托盘图标（纯 ctypes） |
| `paths.py` / `applog.py` / `version.py` | 路径解析 / 滚动日志 + 降级观测 / 统一版本号 |

---

## 6. 功能需求（含实现状态）

### 6.1 软件清单总览与自动分类 —— ✅ 已实现（`inventory.py`）

扫描来源全部落地：注册表卸载项（三处 hive）、开始菜单 `.lnk`、运行中进程；`winget` 列为可选未做。
分类体系落地为 `config.default.json`（单一事实源），用户覆盖放 `config.json`。
输出 `software_inventory.json`（+ 可选 CSV），守护启动刷新、运行中新软件自动补录。

### 6.2 基础使用监控 —— ✅ 已实现（`monitor.py` + `win32core.py`）

- 默认 5 秒轮询（1–30 可配）；`(exe, 标题, AI 归属)` 变化才写一条，静止零写入；
- 空闲默认 3 分钟截断；锁屏/睡眠不计时；
- 会话记录字段与 v1.0 数据模型一致，另增 `url`（浏览器会话关联，best-effort）。

### 6.3 社交软件深度监控 —— ✅ 已实现

- 微信 / QQ / TIM / 钉钉标题解析联系人 + `aliases.json` 别名表；主界面不计联系人；
- 浏览器标题关键词分类（视频/代码/学习，优先级可配）；
- **明确禁止项始终未做**：不读聊天内容、不读聊天数据库。

### 6.4 vibe coding（AI 编程）监控 —— ✅ 已实现并大幅扩展

- 进程树识别（`CreateToolhelp32Snapshot` + BFS）终端里的 AI CLI，`ai_tool` 落字段；
- 关键词匹配避免误伤（python/pip 不会因含 "pi" 误判 —— `tool_registry.py` 精确匹配）；
- v1.0 列为"Phase 3 可选"的**会话深度统计已全面落地**：轮次、Token（真实 usage 优先 / 加权估算）、按模型 / 按项目拆分、成本估算与定价覆盖、质量评分；
- 支持 20+ 本地工具（opencode / ChatGPT / Claude / Cursor / Windsurf / Trae / DeepSeek / Pi Agent / DSH / ZCode / Codex CLI 等）与 4 类 Web AI，矩阵见 `docs/HARNESSES.md`。

### 6.5 数据存储与每日目录结构 —— ✅ 已实现（根目录改为可配置）

- `data_root` 可配置（空 = 程序目录），每日 `YYYY-MM-DD/` 文件夹；
- `usage.jsonl`（必选）/ `software_inventory.json`（必选）/ `report.md`（必选）/ `report.csv`（可选）/ `logs/app.log`（滚动日志 + 降级观测）；
- SQLite 镜像可选，**不替代** JSONL；
- 保留策略默认 90 天可配。

### 6.6 报表与查看 —— ✅ 已实现并扩展

- CLI：`report.py --day/--today/--week/--month`、`--verify/--repair`；
- 托盘：今日概览 / 打开仪表盘 / 检查更新 / 暂停·继续 / 退出；
- 仪表盘：十三视图（概览/趋势/日报/周报/月报/会话/时间轴/成长/对比/日志/分组/洞察/设置），仅 127.0.0.1，Origin/Referer 校验 + 可选口令；
- 新增：周报/月报、数据导出、备份/恢复、AI 洞察、告警闭环、每日目标、成长曲线、工具对比、受限查询、采纳率代理等。

### 6.7 部署、自启动与卸载 —— ✅ 已实现

`installer.ps1` / `uninstaller.ps1`（计划任务登录自启、崩溃重启、可选删数据）；`monitor.py --test N` 测试模式保留。

---

## 7. 性能要求 —— ✅ 达成并持续优化

| 指标 | 目标 | 现状 |
|---|---|---|
| CPU | 静态 ≈0%，活动 <0.3% | 达成（5s 轮询 + 变化才写） |
| 内存 | Python ≤30MB / exe ≤50MB | exe 约 12MB |
| 磁盘 | ≤5MB/天 | 达成 |
| 写入频率 | 仅状态变化写一条 | 达成 |
| 轮询开销 | 单次 3–5 个 Win32 调用 | 达成 |

v2.9.12 性能专项：浏览器 URL 关联免整库拷贝（6.1ms → 0.49ms/次，约 12×）；Git 多日分析区间批量（44 天约 9.2s → 1.4s）；AI 会话/浏览器历史指纹缓存（约 200× / 144×）；SQLite 写入约 66×。

**明确禁止项（截屏/录屏/OCR/键盘记录/剪贴板/ETW/云上传）始终未实现。**

---

## 8. 隐私与安全要求 —— ✅ 已实现并加强

1. 数据只存本机 `data_root`，默认无任何联网上传；
2. 只记元数据；标题黑名单命中记 `[已隐藏]`；
3. 不读聊天内容 / 密码 / 输入 / 文件内容；
4. 仪表盘仅 127.0.0.1 + Origin/Referer 校验 + 可选 `dashboard_token`（HMAC 常量时间比较）；
5. AI 洞察默认关闭，开启仅发送聚合统计（不含标题/URL/联系人）；
6. 更新供应链安全：下载地址白名单（仅 GitHub 官方域名或 `update.api_base` 指定域名）+ SHA256 校验；
7. `config.json` 已取消版本跟踪（防明文 api_key 入库）。

---

## 9. 数据模型 —— ✅ 与实现一致

`usage.jsonl` 每行一个 JSON，字段与 v1.0 表一致（`start/end/duration_ms/exe/app/title/category/contact/ai_tool/active`），浏览器会话另带 `url`。
`software_inventory.json` 结构同 v1.0。

---

## 10. 配置文件 —— ✅ 已实现（热重载）

`config.json`（不存在时用 `config.default.json` 生成）；monitor 每轮重读、mtime 变化即时生效。
`aliases.json` 联系人别名同 v1.0。AI 定价覆盖：`ai_sessions.costs.model_pricing` 或数据目录 `ai_pricing.json`（优先级最高）。

---

## 11. 交付物目录结构 —— ✅ 已实现（按仓库实际布局）

源码仓库即交付物：核心模块 + `installer.ps1`/`uninstaller.ps1` + `config.default.json` + `README.md`/`CHANGELOG`/`docs/` + PyInstaller `VibeTrace.spec`；
运行期在 `data_root` 下生成每日目录与 `usage.db`/`logs/`。Release 由 CI 打 tag 自动构建（附 sha256）。

---

## 12. 技术选型 —— ✅ 按方案 A 落地

Python + ctypes 直调 Win32，**零第三方运行时依赖**（不引入 psutil/pywin32）；
PyInstaller 单文件 exe（`--noconsole`）；进程树用 `CreateToolhelp32Snapshot`，不用 WMI 轮询。
C#/.NET 与 Electron/浏览器扩展方案未采用（同 v1.0 结论）。

---

## 13. 实施阶段与验收标准 —— ✅ 全部达成

- Phase 1（基础版）：✅ v1.0.0 交付，验收点全过；
- Phase 2（深度版）：✅ 联系人/浏览器分类/进程树 AI 识别/托盘，验收点全过；
- Phase 3（增强版）：✅ URL 级历史、周报/月报、AI 会话深度、导出、仪表盘全部落地；
- 演进阶段（v2.x）：AI 编程分析平台（质量评分/时间轴/预算/对比/成长/查询）、行动闭环（告警/目标）、性能专项、框架统一（tool_registry / metrics_util / derived）。

---

## 14. 测试方案 —— ✅ 已体系化

v1.0 的 10 项手动测试点全部保留为自动化回归；现行体系（`docs/TEST_WORKFLOW.md`）：
pytest 七层（unit / integration / api / frontend / performance / security / e2e），**全量 882 passed / 7 skipped**；
`ruff check .` 0 违规；覆盖率门禁 70%（实测约 80%）；CI 在 push、PR、tag 三处触发。

---

## 15. 风险与已知局限 —— 现状

| 风险/局限 | 现状与对策 |
|---|---|
| 管理员窗口标题读不到 | `monitor.py --admin` 可选管理员模式 |
| UWP 标题可能为空 | 按 exe 记录 + `uwp_app_names` 显示名映射 |
| 后台标签页不计时 | 前台注意力口径，文档已说明 |
| AI 会话解析精度 | best-effort，格式差异可能缺失 |
| 杀软误报 | 建议代码签名（未做，无证书） |
| Firefox 停留时长 | 估算值，`firefox_dwell_max_s` 可配 |
| WSL 内 CLI 会话 | 不自动扫描，可显式配 UNC 路径 |
| `shannon_entropy` 负计数 | 理论缺陷，调用点恒非负，characterization 测试钉死 |

---

## 16. 待确认问题清单 —— ✅ 已全部解答

1. **pi agent 进程名**：`tool_registry.py` 精确匹配（pi-agent/piagent/pi_agent），杜绝 python/pip 误伤；
2. **交付形式**：Python 源码 + PyInstaller 单文件 exe（CI 自动构建 Release）；
3. **保留 90 天**：采纳，`retention_days` 可配；
4. **JSONL + SQLite**：JSONL 为原始事实源，SQLite 为可选镜像/索引；
5. **托盘 + 仪表盘**：都做（托盘纯 ctypes；仪表盘十三视图）；
6. **AI 会话深度统计**：做，且为默认开启的核心功能；
7. **微信读本地数据库**：**不做**（同 v1.0 结论，账号风险）；
8. **多用户**：单用户会话设计，未做多用户。

---

## 附录 A：核心 Win32 API 参考

| API | 用途 |
|---|---|
| `user32.GetForegroundWindow` | 前台窗口句柄 |
| `user32.GetWindowThreadProcessId` | 窗口 → 进程 PID |
| `user32.GetWindowTextW` / `GetWindowTextLengthW` | 窗口标题 |
| `user32.GetLastInputInfo` | 空闲检测 |
| `kernel32.CreateToolhelp32Snapshot` + `Process32FirstW/NextW` | 枚举进程（pid/父pid/exe） |
| `kernel32.GetTickCount` | 空闲时长计算 |
| `winreg` | 软件清单注册表扫描 |

（与 `win32core.py` 实现一一对应。）

## 附录 B：默认分类词典

以 `config.default.json` 为单一事实源（v1.0 草案已全部落地并扩充），用户覆盖放 `config.json`。

---

*本需求以"可执行、可验收"为原则编写。v2.0 起与实现保持对齐：需求章节标注实现状态，实现偏离以本文档为准并同步更新。*
